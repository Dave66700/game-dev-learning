这是 划分字母区间 问题：

给你一个字符串 s，要把它划分为尽可能多的片段，使得同一字母最多出现在一个片段中。

返回每个片段的长度列表。

思路：

贪心算法+区间合并

核心思路：

1、 记录每个字母最后出现的位置

先遍历一次字符串，记录每个字母最后一次出现的下标位置

2、贪心划分

再次遍历

我们要维护起始位置start和结束位置end

更新当前片段的结束位置：max（end，last[当前字符]）

当遍历到 i == end 说明当前片段结束了

计算当前的长度，添加结果，最后加入
